export type OperationOriginalDataType = {
    length?: number,
      id: string | null,
    type: string | null,
    category: string | null,
    amount: number | string,
    date: string,
    comment: string | null,
    title?: string | null,
}

